{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^clean", "clean", "^generate", "generate", "^build"],
      "inputs": ["src/**/*.ts", "package.json", "tsconfig.json"],
      "outputs": ["dist/**"],
      "outputLogs": "errors-only"
    },
    "typecheck": {
      "dependsOn": ["^build", "^typecheck"],
      "inputs": ["src/**/*.ts", "package.json", "tsconfig.json"],
      "outputs": [],
      "outputLogs": "errors-only"
    },
    "format": {
      "outputLogs": "errors-only"
    },
    "format:check": {
      "outputLogs": "errors-only"
    },
    "lint": {
      "inputs": ["src/**/*.ts"],
      "outputLogs": "errors-only"
    },
    "lint:check": {
      "inputs": ["src/**/*.ts"],
      "outputLogs": "errors-only"
    },
    "test": {
      "dependsOn": ["^build", "^test"],
      "inputs": ["src/**/*.ts"],
      "outputLogs": "full"
    },
    "test:coverage": {
      "dependsOn": ["^build", "^test"],
      "inputs": ["src/**/*.ts"],
      "outputs": ["coverage/**"],
      "outputLogs": "errors-only"
    },
    "generate": {
      "inputs": [
        "avro/**/*.json",
        "openapi/**/*.yaml",
        "openapi/**/*.yaml.template"
      ],
      "outputs": ["src/generated/**/*.ts"],
      "outputLogs": "errors-only"
    },
    "version": {},
    "clean": {
      "inputs": ["dist/**"],
      "cache": false
    }
  }
}
