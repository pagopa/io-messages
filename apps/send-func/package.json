{
  "name": "send-func",
  "main": "dist/func.js",
  "type": "module",
  "files": [
    "dist/*",
    "host.json",
    "package.json"
  ],
  "version": "1.5.9",
  "scripts": {
    "clean": "shx rm -rf dist",
    "build": "esbuild --bundle ./src/func.ts --format=esm --platform=node --target=node22 --outdir=dist --packages=external",
    "build:watch": "pnpm run build --watch",
    "start": "func start --port 7074",
    "format": "prettier --write .",
    "lint": "eslint --fix src",
    "lint:check": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "@azure/functions": "catalog:",
    "applicationinsights": "catalog:",
    "io-messages-common": "workspace:^",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@pagopa/eslint-config": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "esbuild": "catalog:",
    "eslint": "catalog:",
    "prettier": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "license": "UNLICENSED"
}
