{
  "name": "payment-updater",
  "license": "UNLICENSED",
  "version": "2.7.0",
  "type": "module",
  "scripts": {
    "build": "mvn clean package",
    "test": "mvn test",
    "test:coverage": "mvn test jacoco:report",
    "start": "mvn spring-boot:run",
    "format": "mvn fmt:format",
    "format:check": "mvn fmt:check",
    "version": "mvn versions:set -DnewVersion=$npm_package_version"
  }
}
