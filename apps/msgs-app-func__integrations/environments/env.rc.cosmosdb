COSMOSDB_URI=${REMOTE_CONTENT_COSMOSDB_URI}
COSMOSDB_KEY=${REMOTE_CONTENT_COSMOSDB_KEY}
COSMOSDB_NAME=${REMOTE_CONTENT_COSMOSDB_NAME}

# needed to connect to cosmosdb server
NODE_TLS_REJECT_UNAUTHORIZED=0
