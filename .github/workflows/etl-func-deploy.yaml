name: Deploy (etl-func)

on:
  workflow_dispatch:

jobs:
  etl_func_deploy:
    uses: pagopa/dx/.github/workflows/web_app_deploy.yaml@add-web-app-deploy-workflow
    name: Deploy
    secrets: inherit
    with:
      workspace_name: etl-func
      environment: app-prod
      resource_group_name: io-p-itn-msgs-rg-01
      web_app_name: io-p-itn-msgs-etl-func-01
      health_check_path: /health
      use_staging_slot: false
      use_private_agent: true
